# Core Django
Django==4.2.7

# Database
psycopg2-binary==2.9.7   # PostgreSQL support
#OR
# (SQLite3 is built-in, no package needed)

# Forms & Templates
django-crispy-forms==2.0.2
django-widget-tweaks==1.5.4

# Image & Media Handling
Pillow==10.0.1

# For environment variables (.env file)
python-dotenv==1.0.0

# Optional: If you used email functionality
django-environ==1.3.0

# Optional: For development and debugging
ipython==8.16.2
django-debug-toolbar==4.1

# Optional: Security / password hashing
bcrypt==4.0.1
